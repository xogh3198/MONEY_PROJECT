-- 배당금 봇 시드 데이터 (테스트용)
-- JPA가 테이블 생성 후 Spring Boot의 data.sql로 실행됨
-- 이 파일은 Docker 환경에서 직접 사용하지 않음 (JPA ddl-auto=update 이후 실행)

-- 주의: 테이블은 JPA가 자동 생성하므로 CREATE TABLE 없음
-- 이 파일은 참조용이며, 실제 시드는 src/main/resources/data.sql에 배치
